import {Spinner} from "@/components";

const SearchResult = () => {
    return (
        <section className="bg-card p-8 text-3xl">
            <h1>searchResult</h1>
            <Spinner/>
        </section>
    );
};

export default SearchResult;